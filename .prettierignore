# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# swc runtime configs — JSON content, but the extension has no prettier parser
**/*.swcrc

# Docker build files — no prettier parser for Dockerfile / .dockerignore
Dockerfile
.dockerignore
